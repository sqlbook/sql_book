#!/usr/bin/env sh

# Local convenience wrapper for development startup.
export POSTGRES_HOST="${POSTGRES_HOST:-localhost}"
export POSTGRES_PASSWORD="${POSTGRES_PASSWORD:-password}"
export POSTGRES_READONLY_PASSWORD="${POSTGRES_READONLY_PASSWORD:-password}"

exec bin/dev "$@"
